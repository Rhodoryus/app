// lib/pages/financeiro_screen.dart

import 'package:flutter/material.dart';

class FinanceiroScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Controle Financeiro')),
      body: Center(
        child: Text('Conteúdo sobre controle financeiro'),
      ),
    );
  }
}
