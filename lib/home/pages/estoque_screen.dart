// lib/pages/estoque_screen.dart

import 'package:flutter/material.dart';

class EstoqueScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gerenciar Estoque')),
      body: Center(
        child: Text('Conteúdo sobre gerenciamento de estoque'),
      ),
    );
  }
}
