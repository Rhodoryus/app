// lib/pages/plantio_screen.dart

import 'package:flutter/material.dart';

class PlantioScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gerenciar Plantio')),
      body: Center(
        child: Text('Conteúdo sobre gerenciamento de plantio'),
      ),
    );
  }
}
