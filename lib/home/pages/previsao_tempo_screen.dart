// lib/pages/previsao_tempo_screen.dart

import 'package:flutter/material.dart';

class PrevisaoTempoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Previsão do Tempo')),
      body: Center(
        child: Text('Informações sobre previsão do tempo'),
      ),
    );
  }
}
