import 'package:flutter/material.dart';

class FinancasScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Controle de Finanças')),
      body: Center(child: Text('Acompanhe suas finanças aqui!')),
    );
  }
}
