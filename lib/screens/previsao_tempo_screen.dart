import 'package:flutter/material.dart';

class PrevisaoTempoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Previsão do Tempo')),
      body: Center(child: Text('Dados de previsão do tempo aqui!')),
    );
  }
}
